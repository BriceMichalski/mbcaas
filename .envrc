source ~/.envrc
export MBCAAS_ROOT_DIR="$(expand_path .)"
export KUBECONFIG=$(expand_path ./kubeconfig-prod)
export SOPS_AGE_KEY_FILE=$(expand_path ./keys.txt)
export MBCAAS_SOPS_FILE=$(expand_path ./keys.txt)
export PATH="$PATH:$(expand_path ./bin)"
