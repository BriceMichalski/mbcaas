---
ports:
    web:
        port: 80
    websecure:
        port: 443
        tls: 
            enabled: {{ traefik_web_secure_enable_tls }}
logs:
    general:
        level: {{ traefik_log_level }}
    access:
        enabled: {{ traefik_access_log }}