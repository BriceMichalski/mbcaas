---
apiVersion: kci.rocks/v1alpha1
kind: DbInstance
metadata:
  name: postgres-shared
  namespace: "{{ kube_data_namespace }}"
spec:
  adminSecretRef:
    Name: postgres-secret
    Namespace: "{{ kube_data_namespace }}"
  engine: postgres
  generic:
    host: postgresql
    port: 5432